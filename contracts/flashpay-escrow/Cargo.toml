[package]
name = "flashpay-escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "23.4.1"

[dev-dependencies]
soroban-sdk = { version = "23.4.1", features = ["testutils"] }
